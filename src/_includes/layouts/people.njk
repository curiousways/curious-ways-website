{% extends "layouts/base.njk" %} 
{% set pageCriticalStyles = ['css/people.css'] %}
{% set bodyClass = 'page-people' %}
{% set pageHeaderTitle = title %} 
{% set pageHeaderSummary = content %} 
{% set peopleItems = collections.people %} 

{% block content %}

  <article>
    {% include "partials/page-header.njk" %} 
  </article>

  {% if peopleItems %} 
    <div class="[ no-js-hidden ] [ panel ]">
      <div class="[ wrapper ] [ flow flow-space-700 ]">
        {% include "partials/people.njk" %}
      </div>
    </div>
  {% endif %}

  <div class="wrapper">
    {% include "partials/living-wage.njk" %} 
    <p class="measure-short gap-top-600">Actually, we pay the London Living Wage as a minimum, regardless of where people live. We're not saying this to brag - it really is the least we can do - but because it might inspire you to do the same (if you're not already). Check out <a href='https://livingwage.org.uk' target='_blank' rel='noopener'>livingwage.org.uk</a></p>
  </div>

  {% include "partials/cta.njk" %}

{% endblock %}